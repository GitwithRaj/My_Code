#include<stdio.h>
int main()
{
  long long int a,b,sub,add;
  printf("enter the no.atleast 10 digit of each: ");//valid upto 20 digit
  scanf("%lld%lld",&a,&b);
  // add=a+b;
  // sub=a-b;
  printf("%d",&a);
  // printf("the add of %lld + %lld is %lld\n ",a,b,add);
  // printf("the subtract of %lld - %lld is %lld \n",a,b,sub);
}